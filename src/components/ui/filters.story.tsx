import { Meta } from "@storybook/react";
import { Filters } from "./filters";

export default {
  title: "Components/Filters",
  component: Filters,
} as Meta;

export const Primary = () => <Filters />;
