import { Meta } from "@storybook/react";
import { AccountForm } from "./account-form";

export default {
  title: "Forms/AccountForm",
  component: AccountForm,
} as Meta;

export const Default = () => <AccountForm />;
