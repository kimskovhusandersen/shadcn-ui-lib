import { Meta } from "@storybook/react";
import { ProfileForm } from "../components/ui/form.story";

export default {
  title: "Forms/ProfileForm",
  component: ProfileForm,
} as Meta;

export const Default = () => <ProfileForm />;
